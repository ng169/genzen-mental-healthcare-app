import 'package:flutter/material.dart';

class UserDataWidger extends StatelessWidget {
  const UserDataWidger({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
